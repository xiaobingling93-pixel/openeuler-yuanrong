.. _is_exception:

yr.object_ref.ObjectRef.is_exception
--------------------------------------

.. py:method:: ObjectRef.is_exception() -> bool

    未来任务是否发生异常。

    返回：
        未来任务是否发生异常。数据类型为 bool。
