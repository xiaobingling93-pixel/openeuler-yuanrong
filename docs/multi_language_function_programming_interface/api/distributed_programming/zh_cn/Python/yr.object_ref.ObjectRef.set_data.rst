.. _set_data:

yr.object_ref.ObjectRef.set_data
---------------------------------------------------

.. py:method:: ObjectRef.set_data(data)

    设置数据。

    参数：
        - **data** (ObjectRef) - 用户回调函数。

