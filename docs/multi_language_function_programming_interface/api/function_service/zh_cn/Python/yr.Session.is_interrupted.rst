.. _is_interrupted:

yr.Session.is_interrupted
-------------------------------

.. py:method:: Session.is_interrupted()

    检查当前会话是否已被外部中断。

    返回：
        如果已中断则返回 ``True``，否则返回 ``False``。
        数据类型：bool。
