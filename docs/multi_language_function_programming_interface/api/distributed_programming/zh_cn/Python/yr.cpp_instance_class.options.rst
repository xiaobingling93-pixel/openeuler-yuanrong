.. _options:

yr.cpp_instance_class.options
------------------------------

.. py:method:: cpp_instance_class.options(invoke_options: InvokeOptions) -> None

    设置用户调用选项。

    参数：
        - **invoke_options** (InvokeOptions) – 调用用户设置资源的选项。

    返回：
        None。

