.. _set_session_id:

yr.Context.set_session_id
-------------------------

.. py:method:: Context.set_session_id(session_id)

    设置当前调用的 session ID。

    参数：
        - **session_id** (str) - session ID 字符串。
