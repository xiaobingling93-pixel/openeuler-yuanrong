.. _cancel_ObjectRef:

yr.object_ref.ObjectRef.cancel
------------------------------------------------

.. py:method:: ObjectRef.cancel()

    取消对象的未来任务。

    返回：
        如果未来任务已被取消，则返回 True，否则返回 False；如果未来任务正在运行或已经完成，则无法取消。数据类型为 bool。
