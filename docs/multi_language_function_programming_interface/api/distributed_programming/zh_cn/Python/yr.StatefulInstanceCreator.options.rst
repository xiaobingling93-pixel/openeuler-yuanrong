.. _options_sic:

yr.StatefulInstanceCreator.options
----------------------------------

.. py:method:: StatefulInstanceCreator.options(*args, **kwargs)

    设置实例创建选项。

    参数：
        - **\*args** - 可变参数，用于传递非关键字参数。
        - **\*\*kwargs** - 可变参数，用于传递关键字参数。

    返回：
        实例选项包装器。
